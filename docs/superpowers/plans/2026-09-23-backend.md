# AI Sana backend Implementation Plan

> For agentic workers: use subagent-driven-development for bounded independent implementation and reviews. User authorized complete implementation in this session. No repeated approval gate.

**Goal:** Deliver developer B's complete P0/P1 backend and a usable frontend integration contract.

**Architecture:** A single modular Express server with screen presenters, domain services and SQLite transactions. OpenAI and Git providers are injectable. HTTP commands persist before Socket.IO invalidation.

**Tech Stack:** Node22+, TypeScript, Express5, better-sqlite3, Socket.IO, Zod, OpenAI SDK, Vitest/Supertest.

## Tasks

- [x] 1. Scaffold backend package, tsconfig, scripts, env example and ignores; install dependencies. Files backend/package.json, backend/tsconfig.json, .gitignore.
- [x] 2. Define contracts and score rules. Files backend/src/contracts.ts, domain/score.ts; test/score.test.ts. Test boundary levels/unknown data/decreasing score before implementation. Run npm test -- score.
- [x] 3. Implement provider interfaces and OpenAI/Git adapters with deterministic tests. Files backend/src/integrations/*, test/integrations.test.ts. Reject malformed AI questions, degrade visibly on remote failure, never award points.
- [x] 4. SQLite schema/store, session auth and seed. Files backend/src/db.ts, auth.ts, seed.ts. Test restart, ownership, expiry, seed idempotence.
- [x] 5. Domain services, views and routes. Files backend/src/services/*, views.ts, app.ts. Integration test cafe journey, two teams, multiple selections, P1 approval once, stale versions and privacy. Run npm test -- journeys.
- [x] 6. Realtime and executable server. Files backend/src/server.ts, realtime.ts. Test two clients, private room filtering and reconnect/refetch. Run npm test -- realtime.
- [x] 7. Deliver client SDK, OpenAPI and interactive docs, README and frontend handoff; demo smoke script. Files backend/client/*, backend/src/openapi.ts, backend/README.md, docs/backend-integration.md, backend/scripts/demo.ts.
- [x] 8. Run npm run check, npm test, npm run build, npm run demo, npm audit --omit=dev. Review actual changes, fix material issues, commit on backend/bff. No merge or deployment.

## Acceptance

All tests pass. Demo creates a new task through HTTP, publishes, gets two proposals, selects both, submits evidence, approves and verifies a single 10 point award. Data survives reopening SQLite. OpenAPI request schemas match runtime validators. SDK compiles without server dependencies. Source links, AI prompt and fallback limitations are documented.

## Verification record

2026-09-23: clean npm ci; format:check, TypeScript check, 111 tests in 7 files, and production build passed. Live HTTP demo passed with OpenAI clarification, OpenAI evidence review, public GitHub metadata, two selected teams and exactly-once award. Swagger rendered all 24 operations without browser console or schema errors. Final targeted review approved. Credentials and SQLite remain ignored local files.

2026-09-23 UX fixes: resolved all five review items. Clarification progress and saved answers survive individual saves; new clarification sessions on confirmed cards remain active until reviewed; equivalent success criteria skip redundant questions. Pending review markers and new teams now update public views through sockets. Team activity no longer conflicts with editor versions. Idempotency and score ledger SQL moved into Store. Added regression coverage before fixes: 119 tests in 7 files now pass, along with TypeScript, formatting, production build and the isolated HTTP demo using stub/mock providers. Independent spec review rechecked the edge cases. Swagger and frontend handoff include the updated BFF fields and event/version semantics.
